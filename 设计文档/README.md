# 设计文档

这里存放项目设计材料，包括正式设计决策、人类用户的思路与草稿。

## 读取原则

- 人类用户放在这里的思路与草稿，不会自动视为正式设计。
- 只有用户明确声明、或对话中已经达成共识的内容，才会写入正式决策记录。
- 正式决策记录只追加，不删除、不改写历史痕迹。
- 如果新的设计要求与旧决策冲突，先提示冲突并等待用户确认。

## 文件

- `设计决策记录.md`：正式设计决策的追加式记录。
- `版本记录.md`：玩家可见版本的发布记录；存档结构版本仍由游戏代码中的 `SAVE_VERSION` 单独维护。
